DAMOLDA emotion theme v2 patch

Copy these files over your current project root.

Changed:
- app.js: theme detail labels no longer show Coming Soon/status text.
- style.css: emotion theme detail layout refined for the new text-free background.
- assets/theme-backgrounds/emotion.png: replaced with the new warm minimal gallery background.

HTML files are included with canonical names because uploaded index was named index(6).html and data file was named data(3).js.
